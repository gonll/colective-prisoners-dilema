export const numberOfRandomPrisoners: number = 200; // How many random prisooners do we wanna use?
export const minRounds: number = 100; //Prisoners should compete at least this ammount of times with each other.
export const maxRounds: number = 200; //Prisoners should compete at max this ammount of times with each other.
export const showAmmountResults: number = 3; // How many prisoners should be listed in podium.

export const ammountOfGames: number = 5; // How many times the game should run.
export const runAllStrats: boolean = true; // Runs all available strats against each other. Won't create random nor repeated prisoners.