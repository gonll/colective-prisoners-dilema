/// SETTINGS
export const numberOfRandomPrisoners = 500; // How many random prisooners do we wanna use?
export const minRounds = 100; //Prisoners should compete at least this ammount of times with each other.
export const maxRounds = 250; //Prisoners should compete at max this ammount of times with each other.
export const showAmmountResults = 200; // How many prisoners should be listed in podium

export const ammountOfGames = 1; // How many times the game should run.
export const runAllStrats = true; // Runs all available strats against each other. Won't create random nor repeated prisoners.