/// SETTINGS
export const numberOfRandomPrisoners = 100; // How many random prisooners do we wanna use?
export const minRounds = 150; //Prisoners should compete at least this ammount of times with each other.
export const maxRounds = 300; //Prisoners should compete at max this ammount of times with each other.
export const showAmmountResults = 20; // How many prisoners should be listed in podium

export const ammountOfGames = 5; // How many times the game should run.
export const runAllStrats = true; // Runs all available strats against each other. Won't create random nor repeated prisoners.